// 中文(简体)、英语（美国）、越南语、印度尼西亚语、马来语(马来西亚)
const languageCode = ['zh-CN', 'en-US', 'vi-VN', 'id-ID', 'ms-MY']

// json文件格式
const JSON_EXTS = {
  'JSON': '.json',
  'JSON5': '.json5'
}
module.exports = {
  languageCode,
  JSON_EXTS,
}